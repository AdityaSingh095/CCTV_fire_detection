## Dataset

This project uses a dataset sourced from Kaggle. You can access the dataset on [Kaggle](https://www.kaggle.com/datasets/tharakan684/urecamain). The dataset is organized into three directories:

- `Train` - Contains images labeled as `Fire` and `Non-Fire` for training the model.
- `Validation` - Contains images for validating the model performance.
- `Test` - Contains images for testing the model.
